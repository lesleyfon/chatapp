{
  "scripts": {
    "prepare": "husky",
    "lint": "biome check --write --organize-imports-enabled=false --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "husky": "^9.1.7",
    "lint-staged": "^12.3.2"
  },
  "lint-staged": {
    "*.{ts,tsx,json,jsonc}": [
      "biome check --write --organize-imports-enabled=false --no-errors-on-unmatched"
    ]
  }
}
